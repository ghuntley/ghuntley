[package]
name = "license"
version = "0.1.0"
edition = "2024"
description = "A program which ensures source code files have copyright license headers"
authors = ["Otter Infrastructure"]

[dependencies]
clap = { version = "4.4", features = ["derive"] }
regex = "1.10"
walkdir = "2.4"
glob = "0.3"
git2 = "0.20"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
toml = "0.9"
ignore = "0.4"

[build-dependencies]
# No build dependencies needed for include_str! macro
